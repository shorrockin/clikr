package main

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestHandlers(t *testing.T) {
	Convey("the handlers", t, func() {
		SkipConvey("should be able to set the content type", func() {
		})

		SkipConvey("should be able to retrieve the variables from the url", func() {
		})
	})
}
